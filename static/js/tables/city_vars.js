"use strict";

const table_name = "city";
const fields = ["city_id", "city_name"];
const fields_names = ["ID", "Город"];

const forms = {
    city_name: "text",
};

const form_params = {
    city_name: {
        placeholder: "Введите город",
    },
};
